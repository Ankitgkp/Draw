{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @repo/common build && pnpm --filter @repo/db build && pnpm --filter @repo/backend-common build && pnpm --filter http-backend build"
  },
  "deploy": {
    "startCommand": "cd apps/http-backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
