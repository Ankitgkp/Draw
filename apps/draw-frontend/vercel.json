{
  "framework": "nextjs",
  "installCommand": "Update that file to pnpm 9: corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build"
}
